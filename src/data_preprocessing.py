# this scripts with downlaod data from the website and parse it for machine learning algorithm
import pandas as pd
import numpy as np


def download_review_data(path): ...


def clean_gmap_meta_data(df): ...


def clean_review_data(df): ...
